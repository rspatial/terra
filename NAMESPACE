import(Rcpp)
importFrom(methods, new, validObject)
importFrom(stats, aggregate)
importFrom(graphics, lines, points, image)
import(raster)
exportMethods(aggregate, as.array, as.data.frame, as.matrix, crop, crs, "crs<-", focal, geom, image, lines, mask, ncell, plot, points, rasterize, trim, res, xres, yres, "nrow<-", "ncol<-", yFromRow, xFromCol, colFromX, rowFromY, cellFromXY, cellFromRowCol, xyFromCell, yFromCell, xFromCell, rowColFromCell, rowFromCell, colFromCell, readStart, readStop, subset, values, "values<-", writeStart, writeStop, writeValues, writeRaster)
exportPattern("^[[:alpha:]]+")
useDynLib(terra, .registration = TRUE)
