import(Rcpp)
import(RcppProgress)
importFrom(methods, as)
importFrom(stats, na.omit, predict)
importFrom(graphics, lines, points)
import(raster)
exportMethods("[", "[[", "==", "!=", "!", adjacent, aggregate, area, Arith, as.contour, as.lines, as.points, as.polygons, as.array, as.data.frame, as.factor, as.matrix, atan2, boundaries, buffer, clamp, classify, click, Compare, contour, couldBeLonLat, cover, crop, crs, "crs<-", density, disaggregate, distance, extend, extract, flip, focal, geom, hasValues, hist, head, ifel, init, interpolate, image, isLonLat, isGlobalLonLat, is.factor, levels, lines, mask, Math, mean, merge, ncell, ncol, "ncol<-", nrow, "nrow<-", overlay, pairs, persp, plot, plotRGB, predict, points, rasterize, reduce, res, "res<-", rotate, setMinMax, shift, Summary, subset, t, text, trim, unique, values, "values<-", xres, yres, yFromRow, xFromCol, colFromX, rowFromY, cellFromXY, cellFromRowCol, cellFromRowColCombine, xyFromCell, yFromCell, xFromCell, rowColFromCell, rowFromCell, colFromCell, readStart, readStop, sampleRegular, tail, writeStart, writeStop, writeValues, writeRaster, zonal, zoom)
exportPattern("^[[:alpha:]]+")
useDynLib(terra, .registration=TRUE)
