import(Rcpp)
import(RcppProgress)
importFrom(methods, new, as, validObject)
importFrom(stats, aggregate, na.omit, predict, quantile)
importFrom(graphics, lines, points, image)
importFrom(utils, head, tail)
import(raster)
exportMethods("[", "[[", "==", "!=", "!", adjacent, Arith, as.points, as.polygons, aggregate, area, as.array, as.data.frame, as.factor, as.matrix, atan2, boundaries, buffer, clamp, classify, click, Compare, couldBeLonLat, cover, crop, crs, "crs<-", disaggregate, distance, extend, extract, flip, focal, geom, gridDistance, hasValues, hist, head, ifel, init, interpolate, image, isLonLat, isGlobalLonLat, is.factor, levels, lines, mask, Math, mean, merge, ncell, ncol, "ncol<-", nrow, "nrow<-", overlay, plot, plotRGB, predict, points, rasterize, reduce, res, "res<-", rotate, setMinMax, shift, Summary, subset, t, trim, unique, values, "values<-", xres, yres, yFromRow, xFromCol, colFromX, rowFromY, cellFromXY, cellFromRowCol, cellFromRowColCombine, xyFromCell, yFromCell, xFromCell, rowColFromCell, rowFromCell, colFromCell, readStart, readStop, sampleRegular, tail, writeStart, writeStop, writeValues, writeRaster, zonal, zoom)
exportPattern("^[[:alpha:]]+")
useDynLib(terra, .registration=TRUE)
