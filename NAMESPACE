import(Rcpp)
import(RcppProgress)
importFrom(methods, new, as, validObject)
importFrom(stats, aggregate, na.omit, predict, quantile)
importFrom(graphics, lines, points, image, polypath)
importFrom(utils, head, tail)
import(raster)
exportMethods("[", "[[", "==", "!=", "!", adjacent, Arith, as.points, as.polygons, aggregate, area, as.array, as.data.frame, as.matrix, Compare, clamp, cover, crop, crs, "crs<-", disaggregate, extract, flip, focal, geom, gridDistance, head, ifel, init, image, lines, mask, Math, mean, merge, ncell, ncol, "ncol<-", nrow, "nrow<-", overlay, plot, points, rasterize, reclassify, reduce, res, rotate, shift, Summary, subset, t, trim, unique, values, "values<-", xres, yres, yFromRow, xFromCol, colFromX, rowFromY, cellFromXY, cellFromRowCol, xyFromCell, yFromCell, xFromCell, rowColFromCell, rowFromCell, colFromCell, readStart, readStop, sampleRegular, tail, writeStart, writeStop, writeValues, writeRaster)
exportPattern("^[[:alpha:]]+")
useDynLib(terra, .registration = TRUE)
